#ifndef Unit8H
#define Unit8H
#include <Classes.hpp>
#include <Controls.hpp>
#include <StdCtrls.hpp>
#include <Forms.hpp>

class TForm8 : public TForm
{
__published:    // IDE-managed Components
    TButton *ButtonExecute;
	TEdit *LabelTask;
    TLabel *LabelScore;
    void __fastcall ButtonExecuteClick(TObject *Sender);
private:    // User declarations
    int score;
    bool (TForm8::*currentFunction)(int); // указатель на функцию для текущей задачи
    void GenerateTask(); // генерация нового задания
    bool ValidateAnswer(); // проверка ответа пользователя

    // функции задач
    bool SquareFunction(int number);
    bool FactorialFunction(int number);
    bool IsPrimeFunction(int number);
    bool RandomFunction(int number);

    // вспомогательные функции
    bool PromptForAnswer(int correctAnswer);
    bool PromptForAnswer(bool correctAnswer);

public:        // User declarations
    __fastcall TForm8(TComponent* Owner);
};

extern PACKAGE TForm8 *Form8;
#endif
